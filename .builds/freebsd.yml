image: freebsd/14.x
packages:
  - ca_root_nss
  - curl
  - libedit
  - libssh2
  - libtool
  - libunistring
  - pkg
  - pkgconf
  - readline
  - perl5
sources:
  - https://git.sr.ht/~herrhotzenplotz/gcli
tasks:
  - build: |
      cd gcli
      ./configure --debug
      make -j 4 all

  - check: |
      cd gcli
      ./configure --debug
      make -j 4 check
